name: bjorn_utils
channels:
  - conda-forge
dependencies:
  - biopython
  - black
  - isort
  - mypy
  - numpy
  - pandas
  - pip==21.1.3
  - path
  - pip:
    - gspread
    - more-itertools

# add this back once we get bjorn as a module
# git+https://github.com/andersen-lab/bjorn.git